return {
  "windwp/nvim-ts-autotag",
  enabled = true,
  event = "InsertEnter",
  cond = not vim.g.vscode,

  config = function()
    -- Configuration space
  end,

  init = function()
    -- Initialization space
  end,
}
